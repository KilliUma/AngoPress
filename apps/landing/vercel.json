{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && npx turbo run build --filter=@angopress/landing",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && npm install",
  "env": {
    "API_URL": "@api_url",
    "APP_URL": "@app_url"
  }
}
